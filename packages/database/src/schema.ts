// Infrastructure-level foundation.
// This proves the database migration pipeline works without introducing
// business domain tables (tenants, users, etc.) too early.
// We will add extensions or global enum types here when needed.

export {};
